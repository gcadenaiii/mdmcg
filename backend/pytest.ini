[pytest]
asyncio_mode = auto
pythonpath = .
testpaths = tests
addopts = -v --tb=short
markers =
    integration: requires a live PostgreSQL database (use --run-integration to include)
filterwarnings =
    ignore::DeprecationWarning
    ignore::pytest.PytestUnraisableExceptionWarning
